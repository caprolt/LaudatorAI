# Railway Environment Variables Template
# Copy these to your Railway service Variables tab

# Required (Auto-provided by Railway when you add services)
# DATABASE_URL=postgresql://... (auto-provided when you add PostgreSQL service)
# REDIS_URL=redis://... (auto-provided when you add Redis service)
# PORT=8000 (auto-provided by Railway)
# HOST=0.0.0.0 (auto-provided by Railway)

# Required (Manual setup)
BACKEND_CORS_ORIGINS=https://your-frontend-domain.vercel.app,http://localhost:3000
ENVIRONMENT=production
DEBUG=false

# File Storage Configuration (Choose one option)

# Option 1: AWS S3 (Recommended for Railway)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-1
S3_BUCKET_NAME=laudatorai-files

# Option 2: Cloudflare R2 (S3-compatible, often cheaper)
# AWS_ACCESS_KEY_ID=your-r2-access-key-id
# AWS_SECRET_ACCESS_KEY=your-r2-secret-access-key
# AWS_REGION=auto
# S3_BUCKET_NAME=laudatorai-files
# S3_ENDPOINT_URL=https://your-account-id.r2.cloudflarestorage.com

# Option 3: MinIO (Not recommended for Railway - use for local development only)
# MINIO_ENDPOINT=your-minio-endpoint
# MINIO_ACCESS_KEY=your-minio-access-key
# MINIO_SECRET_KEY=your-minio-secret-key
# MINIO_BUCKET_NAME=laudatorai
# MINIO_SECURE=true

# Optional
OPENAI_API_KEY=your-openai-api-key-here

# Optional: Sentry for error tracking
SENTRY_DSN=your-sentry-dsn
